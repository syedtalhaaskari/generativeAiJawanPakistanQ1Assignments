# Find the Length of Each String in a List
# Objective: Write a Python program that finds and prints the length of each string in a given list.
# Example list
# strings = ["apple", "banana", "cherry", "date"]

strings = ["apple", "banana", "cherry", "date"]

for x in strings:
    print(f"Element: {x}; Length: {len(x)};")